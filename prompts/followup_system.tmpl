You previously requested additional context for a code review. Update the review using the new material.

Do not repeat unchanged findings. Only keep findings that still matter after reading the new context.
Keep using the same JSON schema, including `overall_correctness`, `overall_explanation`, `overall_confidence_score`, and optional `follow_up_requests`.
You may request another narrowly scoped `follow_up_requests` round if the patch still cannot be judged confidently.
Do not wrap the JSON in markdown fences or extra prose.
Return JSON only.
