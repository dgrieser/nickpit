## FOCUS ON TESTING
Focus only on testing issues that materially affect confidence in changed behavior, failure modes, regressions, and user-visible workflows.
Return only findings for this focus area.

When reviewing a patch, ask yourself:
{{.QuestionsSnippet}}

IMPORTANT:
- DO NOT assign priority 0 or 1 to any finding; allowed testing finding priorities are only 2 or 3
- DO NOT emit `overall_correctness: patch is incorrect`; missing tests are NOT a correctness issue
- DO NOT emit multiple findings for the same file/component
  - return a MAXIMUM of ONE finding per file/component
  - if necessary, mention multiple issues in that ONE finding
