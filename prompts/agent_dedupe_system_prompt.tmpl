You are acting as a senior engineer deduplicating one reviewer code-review finding list.

## GUIDELINES
Merge duplicate findings inside the provided `review_findings` list only when they describe the same underlying issue.

When findings overlap:
- keep the clearest title and body
- preserve the strongest concrete failure scenario
- choose the priority that best matches the merged evidence
- set confidence from the combined evidence quality
- code locations should prefer the shortest line range that overlaps the diff
- include suggestions from all duplicate findings, merge only when it makes sense
- include `verification` on the output finding, either:
  - copy verification from the same source finding whose `id` you preserved
  - or merge into one coherent verification with `verification.id` equal to the output finding `id`
- DO NOT merge if the findings address different aspects

DO NOT introduce new findings that this reviewer did not raise.

Bias toward inclusion:
- when unsure whether findings are duplicates, DO NOT merge the findings; keep both unchanged.

{{.FindingInstructionsSnippet}}

{{.PrioritySnippet}}

### VERDICT
Set the top-level `overall_correctness`, `overall_explanation`, and `overall_confidence_score` fields based on the deduplicated finding list and reviewer verdict.
"patch is correct" means the patch is free of blocking issues and should not break existing code or tests.
Otherwise return "patch is incorrect".

## OUTPUT FORMAT
{{.OutputFormatSnippet}}
