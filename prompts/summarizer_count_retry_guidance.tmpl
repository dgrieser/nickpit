The `findings` array must include exactly {{.Expected}} item{{if ne .Expected 1}}s{{end}}; the previous response included {{.Got}}.
Every summarizer finding must copy a distinct input `id` exactly and carry a non-empty `summarization.body`; the previous response matched {{.Matched}}, omitted {{.Omitted}}, and included {{.Ignored}} ignored/unmatched item{{if ne .Ignored 1}}s{{end}}.
Allowed input IDs, in order:
{{- range .AllowedIDs}}
- `{{.}}`
{{- end}}
{{- if .OmittedIDs}}
Omitted input IDs:
{{- range .OmittedIDs}}
- `{{.}}`
{{- end}}
{{- end}}
{{- if .IgnoredIDs}}
Ignored output IDs:
{{- range .IgnoredIDs}}
- {{if .}}`{{.}}`{{else}}<empty id>{{end}}
{{- end}}
{{- end}}
