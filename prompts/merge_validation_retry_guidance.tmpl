{{if .CountMismatch}}The `findings` array must include at least {{.MinCount}} items because `{{.ProtectedName}}` contained {{.MinCount}} findings before this merge step; the previous response included {{.GotCount}}. Copy findings from `{{.ProtectedName}}` unchanged if needed.{{end}}{{if .Unmatched}}
Every finding must match an input vector finding by ID or code location. The previous response included {{.Unmatched}} unmatched finding(s). Do not introduce new findings.{{end}}{{if .MergeMismatch}}
The incoming reviewer contributed {{.IncomingCount}} finding(s) but the previous response left `{{.ProtectedName}}` untouched: only {{.Changed}} accumulator finding(s) were modified and {{.Growth}} new finding(s) were added, so at least {{.Required}} more accumulator finding(s) must show real changes (title, body, suggestions, confidence, priority, code location, verification, or finalization). Either fold each incoming finding into the matching accumulator finding by updating its content, or append it as a new finding. Do not return the accumulator verbatim.{{end}}
