{{if .CountMismatch}}The `findings` array must include at least {{.MinCount}} items because `{{.ProtectedName}}` contained {{.MinCount}} findings before this merge step; the previous response included {{.GotCount}}. Copy findings from `{{.ProtectedName}}` unchanged if needed.{{end}}{{if .Unmatched}}
Every finding must match an input vector finding by ID or code location. The previous response included {{.Unmatched}} unmatched finding{{if ne .Unmatched 1}}s{{end}}. Do not introduce new findings.{{end}}{{if .MergeMismatch}}
The incoming reviewer contributed {{.IncomingCount}} finding{{if ne .IncomingCount 1}}s{{end}} but the previous response left `{{.ProtectedName}}` untouched: only {{.Changed}} accumulator finding{{if ne .Changed 1}}s{{end}} were modified and {{.Growth}} new finding{{if ne .Growth 1}}s{{end}} were added, so at least {{.Required}} more accumulator finding{{if ne .Required 1}}s{{end}} must show real changes (e.g. title, body, suggestions, confidence, priority, ...). Either fold each incoming finding into the matching accumulator finding by updating its content, or append it as a new finding. Do not return the accumulator verbatim.{{end}}
