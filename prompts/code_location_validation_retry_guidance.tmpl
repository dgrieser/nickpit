The previous JSON had `code_location` objects whose `content` did not match the referenced file and line range.

Fix every listed location by calling the `find_lines` tool with the exact code snippet and the intended repo-relative path, then copy `matches[].code_location` exactly into the JSON response.

Do not invent file paths, line numbers, line counts, or code snippets. `code_location` must include `file_path`, `line_range.start`, `line_range.end`, `line_range.count`, and `content`; include `language` when `find_lines` returns it.

Invalid locations:
{{- range .Issues}}
- `{{.Field}}`: {{.Reason}}
{{- end}}
